#!/usr/bin/env python3
"""Run the TCP fallback keyboard controller on the Host."""

from tcp_framework.host.keyboard_control import run_keyboard_control


if __name__ == "__main__":
    run_keyboard_control()
